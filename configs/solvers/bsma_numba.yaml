solver_id: bsma_numba
solver_class: BSMANumbaSolver
capabilities:
  problem_types: [mkp]
  encodings: [binary]
  directions: [max]
stop_condition:
  type: max_iterations
  max_iterations: 1000
  max_seconds: null
params:
  # 選填：ctf 與 bsma 相同；省略時 tanh_abs
  # ctf: tanh_abs
  pop_size: 20
  z: 0.08
