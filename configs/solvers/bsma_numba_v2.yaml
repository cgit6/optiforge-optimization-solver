solver_id: bsma_numba_v2
solver_class: BSMANumbaSolverV2
capabilities:
  problem_types: [mkp]
  encodings: [binary]
  directions: [max]
stop_condition:
  type: max_iterations
  max_iterations: 5000
  max_seconds: null
params:
  - pop_size: 20
    z: 0.08
    # 選填：ctf 與 bsma 相同；省略時 tanh_abs
    ctf: tanh_abs
