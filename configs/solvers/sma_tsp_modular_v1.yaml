solver_id: sma_tsp_modular_v1
solver_class: SMATSPModularV1Solver
capabilities:
  problem_types: [tsp]
  encodings: [permutation]
  directions: [min]
stop_condition:
  type: max_iterations
  max_iterations: 500
  max_seconds: null
params:
  - pop_size: 30
    z: 0.08
