### [2026-04-26 17:00] 題庫載入前移至 Engine：Catalog／SHM ProblemBank
**里程碑/事件類型**：架構設計 / 效能基礎
**內容摘要**：
- 事件描述：`Engine.build` 對 `problem_root` 全庫掃描並逐題驗證；產出僅 metadata 之 catalog 與 `GameSetting`；本次實驗題目寫入 `multiprocessing.shared_memory` 由 `ProblemBank` 持有；`Simulator`（含 `worker_curriculum` 子行程）執行期不再讀題目 yaml。
- 原因與背景：對齊「組裝期 fail-fast、執行期共用唯讀題資料」之計畫，並讓多 process 共用同一題面記憶體。
- 對後續影響：CLI／測試需在組裝階段即確保題目合法；大題庫時 SHM 僅涵蓋本次 `problem_ids` 以降低記憶體壓力。
- 關聯紀錄：`log/對話紀錄.md`、`log/程式碼更新紀錄.md` 2026-04-26 17:00；`note/模擬系統.md` 題庫與 Engine／Simulator 段落

### [2026-04-25 11:28] worker_curriculum 執行模型調整：thread → process 優先
**里程碑/事件類型**：效能修正 / 架構調整
**內容摘要**：
- 事件描述：`worker_curriculum` 對內建 solver 改採多行程優先，避免 CPU-bound 演算法在 thread 下受 GIL 影響而出現「像卡住」
- 原因與背景：使用者在 `repeat=20` 壓測回報降迭代仍長時間無明顯進度
- 對後續影響：多核 CPU 下可改善並行吞吐；自訂 solver/測試場景維持 thread fallback 相容性
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md` 與 `log/程式碼更新紀錄.md` 2026-04-25 11:28 條目

### [2026-04-25 17:30] worker_curriculum 並行執行（repeat = 同時線數）
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：`worker_curriculum` 模式下 `Simulator.run_batch` 以 `repeat` 為 worker 線數並行執行，線內仍串行跑完該線之 curriculum；輸出與題庫快取加鎖
- 原因與背景：與使用者對「N 次獨立模擬 = N 線同跑」之期望對齊
- 對後續影響：CPU 密集若需真正多核可再引入 ProcessPool 等選項
- 關聯紀錄：`log/程式碼更新紀錄.md` 2026-04-25 17:30；`log/對話紀錄.md`；`note/模擬系統.md`

### [2026-04-25 16:00] Simulator 任務展開：grid 與 worker_curriculum
**里程碑/事件類型**：新功能 / 開發規範
**內容摘要**：
- 事件描述：`ExperimentSpec` 新增 `execution_mode`；`Simulator.expand_tasks` 支援 grid 與 worker_curriculum 兩種列舉順序，並以典範三元組排序配種，使同一 `(p, s, r)` 跨模式 seed 不變；CLI 新增 `--execution-mode`
- 原因與背景：支援「同一 solver 線依序巡題」的執行順序，同時維持與既有 grid 種子相容
- 對後續影響：平行 worker 或更細 curriculum 參數可再擴充；預設行為不變
- 關聯紀錄（對話或程式碼更新）：`log/程式碼更新紀錄.md` 2026-04-25 16:00 條目；`log/對話紀錄.md`；`note/模擬系統.md` 亂數與主流程段落

### [2026-04-25 14:30] 引入 Engine 與 SimulationBundle（組裝層具名化）
**里程碑/事件類型**：架構設計 / 重構
**內容摘要**：
- 事件描述：新增 `mkp/engine.py`，由 `Engine.build` 組裝 Repository／Registry／Loader／Validator／Writer／Simulator，回傳 `SimulationBundle`；`app` 僅負責 CLI、`ExperimentSpec`、`preflight_validate` 與呼叫 `Engine.build` 後執行 `run_batch`
- 原因與背景：與 `note/模擬系統.md` 中「系統層 Engine」敘述對齊，避免組裝邏輯堆在 Command 模組
- 對後續影響：後續可將 RNG 政策、solver 註冊擴充等逐步收斂至 Engine，無須再動 CLI 結構
- 關聯紀錄（對話或程式碼更新）：`log/程式碼更新紀錄.md` 2026-04-25 條目；`note/模擬系統.md` Engine／bundle 段落

### [2026-04-24 23:21] Converter 模組套件化與 dat 轉換註冊表
**里程碑/事件類型**：重構 / 架構設計
**內容摘要**：
- 事件描述：將 `problem_dat_converter` 重構為 `mkp.converter` 子套件，導入 converter 名稱註冊機制，並完成全專案 import 遷移（不保留舊模組路徑）
- 原因與背景：便於未來依題庫擴充不同 parser，同時維持 `load_problem_model_from_repo_dat` 等單一對外流程
- 對後續影響：第二階段可於不破壞核心 API 的前提下增加 **Converter CLI** 與更多 `register_converter` 實作；安裝組態需包含 `mkp*` 以打包子套件
- 關聯紀錄（對話或程式碼更新）：`log/程式碼更新紀錄.md` 2026-04-24 條目；`note/模擬系統.md` 第二階段說明

### [2026-04-22 21:03] 第一模組完成：核心資料契約落地
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：完成模擬系統第一模組（核心資料契約）並建立第一批單元測試
- 原因與背景：依規劃需先穩定資料契約，再逐步銜接 Repository、SolverRegistry、Simulator 等模組
- 對後續影響：後續模組可直接依賴統一資料結構，降低跨模組耦合與重複解析成本
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 21:24] 第二模組完成：ProblemRepository 落地
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：完成 ProblemRepository（YAML 載入、schema 驗證、best_known fail-fast、快取）與對應單元測試
- 原因與背景：在核心契約完成後，需先建立穩定的資料載入與驗證層，降低後續模擬流程的錯誤來源
- 對後續影響：後續 `Simulator` 可直接依賴 `ProblemRepository.load()` 取得合法 `ProblemModel`，並沿用 fail-fast 契約
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 21:34] 第三模組完成：Solver 介面與 Registry 落地
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：完成 `Solver` 介面、`SolverRegistry`、solver config loader 與對應單元測試
- 原因與背景：在 ProblemRepository 穩定後，需要建立可擴充的 solver 註冊與建立機制，作為後續 Simulator 接入基礎
- 對後續影響：可先以 stub solver 串接主流程，再逐步加入舊演算法 adapter，不需改動呼叫契約
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 21:41] 第四模組完成：Validator 落地
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：完成 `Validator` 與 `ValidationReport`，可進行單次 run 的可行性與 objective 驗證，並標記 best_known 差距
- 原因與背景：在 solver 契約穩定後，需要建立一致的驗證層，作為後續 ResultWriter 與 Summary 的判斷來源
- 對後續影響：後續結果層可直接使用 `ValidationReport` 決定納入/排除統計，減少跨模組重算與分歧
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 21:50] 第五模組完成：ResultWriter / ResultStore 落地
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：完成單次 run 結果寫出與 core summary 聚合，輸出 CSV+JSON，並保留異常 run
- 原因與背景：在 Validator 完成後，需要將 run 結果標準化輸出，作為後續分析與報表基礎
- 對後續影響：後續 Simulator 接入後可直接寫結果；Summary 規則已固定，利於跨 solver 統計比較
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 21:56] 第六模組完成：Simulator 單次 run 落地
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：完成 `Simulator.run_task`，可整合既有載入、求解、驗證、輸出模組完成單次執行
- 原因與背景：在結果層完成後，需要建立主流程協調者，作為後續批次展開與平行執行前置基礎
- 對後續影響：後續可在不改動既有子模組的前提下，擴充任務展開（`problem × solver × repeat`）與 fail-fast 批次策略
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 22:03] 第七模組完成：Simulator 批次展開落地
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：完成 `Simulator` 批次任務展開、seed 預分配與同步 fail-fast 執行
- 原因與背景：單次 run 已穩定，需要補齊批次能力以支援實驗重複與多組比較
- 對後續影響：後續可直接進入 Command/App 參數入口或平行執行優化，不需重寫批次基礎邏輯
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 22:15] 第八模組完成：Command/App 入口落地
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：完成 CLI 入口與前置檢查，正式打通 `CLI -> ExperimentSpec -> Simulator.run_batch`
- 原因與背景：批次執行能力完成後，需要統一的命令層來取代分散腳本入口
- 對後續影響：後續可在此入口擴充真實 solver 註冊、參數驗證、以及第一階段整體驗證流程
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 22:20] 第九模組完成：第一個真實 heuristic 接入
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：完成 `BSMA_V1_008` 真實 solver adapter 接入，並以 `Generator` 重構亂數來源與重現性測試
- 原因與背景：第一階段要從 stub 過渡到真實演算法，驗證框架在真實 solver 下可穩定工作
- 對後續影響：後續可依同模式逐步接入更多 heuristic solver，並維持統一可重現契約
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 22:32] 第十模組完成：第一階段整體驗證與收斂模組
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：完成 stage1 validation runner，打通 smoke 驗證清單、seed 重現驗證、old/new 對照與比較報告輸出
- 原因與背景：第一階段主幹模組已齊，需要收斂性驗證機制來確認重構流程在代表性題目上可穩定重跑並可對照舊版
- 對後續影響：後續可直接擴大 scenario 覆蓋範圍、細化差異分類規則，並以同一 runner 進行持續回歸
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 22:43] 第十模組收斂：old baseline 依賴補齊與相容化
**里程碑/事件類型**：問題修正
**內容摘要**：
- 事件描述：補齊 old 執行依賴並完成相容化執行，讓 stage1 baseline 從不可解析轉為可比較
- 原因與背景：原先 old 腳本在 Linux/現行 repo 狀態下有依賴與模組命名落差，導致 comparison 長期停在 `baseline_missing`
- 對後續影響：第一階段 smoke 比對已可穩定產生有效 baseline，後續可直接擴大到更多 dataset/problem 做收斂驗證
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 22:47] 第十模組收斂：建立 BSMA 嚴格等價判定能力
**里程碑/事件類型**：開發規範
**內容摘要**：
- 事件描述：新增 old/new 同 seed 的中間 trace + 最終結果等價驗證流程，且不改動 `old/*.py`
- 原因與背景：使用者將驗收標準提升為「中間過程與最終結果都一致」，原本僅比最終值的流程不足以判定
- 對後續影響：可作為後續對齊 `mkp/bsma_v1_008_solver.py` 的客觀驗收門檻與回歸檢查基線
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-22 23:02] 第十模組收斂：BSMA 進入對齊模式並達成案例等價
**里程碑/事件類型**：問題修正
**內容摘要**：
- 事件描述：`mkp/bsma_v1_008_solver.py` 由概念性實作改為 legacy adapter，對齊 `old/BSMA2.py` 執行邏輯（不修改 old）
- 原因與背景：需滿足「同 seed 下中間過程與最終結果一致」的嚴格驗收標準
- 對後續影響：可在既有嚴格驗證器上逐步擴大 problem/seed 覆蓋，建立更完整的等價性信心
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-23] 第十模組收斂：BSMA 改為內建 Core 與驗證掛鉤一致
**里程碑/事件類型**：重構
**內容摘要**：
- 事件描述：移除直接委派 `old` 的 adapter，改以 `BSMAV1008Core` 內嵌與 `old/BSMA2` 對齊的實作；嚴格等價驗證改為只對 `BSMAV1008Core` 掛 trace
- 原因與背景：避免「驗證 new 卻 patch 舊模組」與 solver 本體不一致，並符合可審閱、可除錯的對齊流程
- 對後續影響：後續若 scipy 或數值邊界造成漂移，可在 mkp 內單檔除錯並以同一套 equivalence 回歸
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-23] BSMA 對齊 Debug：迭代摘要與多 seed 驗收
**里程碑/事件類型**：開發規範
**內容摘要**：
- 事件描述：依 Debug 計畫擴充嚴格驗證之可觀測性（外層迭代 a/b/W/p/vb/vc 摘要）並完成 `weish01` 三 seeds 驗收
- 原因與背景：需在維持自主實作、不修改 old 的前提下，讓未來 mismatch 可快速判斷是 RNG 序列或公式段落差
- 對後續影響：除錯迭代可「單段修正 → 跑 equivalence（必要時開 loop_meta）→ 看摘要收斂」
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-23 15:30] WEISH 全題庫串流等價驗證與批次報告
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：實作 `verify_equivalence_streaming`、WEISH 批次 runner（預設 seed=101、budget=100_000→max_iter=4999）、輸出 JSON/TXT 摘要；pytest 區分 smoke 與 `@pytest.mark.slow` 全量
- 原因與背景：計畫要求在單一 seed 下對 30 題做與「十萬次適應值上界」對齊的外層迭代驗證，且不得以完整 list 持有 trace
- 對後續影響：本地或 CI 可快速 smoke；完整驗收以 `pytest -m slow` 或批次 CLI 長跑取得 `all_pass` 與每題耗時、停止原因
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`

### [2026-04-24 09:40] 第一階段 Summary 能力收斂
**里程碑/事件類型**：新功能
**內容摘要**：
- 事件描述：完成 summary 模組強化，新增 `overall + by_problem_solver` 聚合、best-known 指標與 `runtime_error` 排除統計，並於 batch 流程結束自動落檔
- 原因與背景：先前 summary 僅整批粗粒度統計，難以直接回答「每題/每 solver」比較與排除原因
- 對後續影響：第一階段報表可直接用於分析與回歸；未來 benchmark 對照可在現有分組結構上擴充
- 關聯紀錄（對話或程式碼更新）：見 `log/對話紀錄.md`、`log/程式碼更新紀錄.md`
