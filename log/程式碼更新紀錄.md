### [2026-04-22 21:03] 建立核心資料契約與 pytest 驗證基線
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/__init__.py`、`mkp/contracts.py`、`tests/test_contracts.py`
- 主要變更內容：
- 新增四個核心資料契約：`ExperimentSpec`、`ProblemModel`、`RunTask`、`RunResult`
- 在契約層加入最小必要驗證（欄位不可空、數值範圍、維度一致性）
- 對 `ProblemModel` 與 `RunResult` 的陣列欄位設為唯讀，避免後續流程意外修改
- 建立 pytest 測試基線並補上 22 個測試案例，涵蓋合法與非法輸入情境
- 相關 Issue 或對話連結：本次「第一模組落地計畫（核心資料契約）」
- 是否通過測試：已通過（`tests/test_contracts.py` 全數通過）
- 備註：測試環境使用專案本地 `.venv`；系統 Python 因 externally-managed 限制不直接安裝套件

**變更前後差異說明（選填）**：
- 前版本簡述：專案以資料集腳本為主，尚無統一契約與測試基線
- 後版本簡述：已可重用核心資料契約，並具備可持續擴充的 pytest 驗證入口

### [2026-04-22 21:24] 新增 ProblemRepository 與資料載入驗證
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/problem_repository.py`、`mkp/__init__.py`、`tests/test_problem_repository.py`
- 主要變更內容：
- 新增 `ProblemRepository.load(dataset, problem_id)`，支援標準路徑載入與快取
- 使用 `ruamel.yaml` 解析 YAML，並加入 YAML 格式錯誤處理
- 實作 schema 驗證：必要欄位、`values/items`、`weights/items×dim`、`capacities/dim`
- 實作 `best_known: null` fail-fast（在 repository 載入階段即拒絕）
- 新增 UT-PRB-001~005 對應測試（合法載入、缺欄位、維度錯誤、best_known null、快取命中）
- 相關 Issue 或對話連結：本次「第二模組落地計畫（ProblemRepository）」
- 是否通過測試：已通過（`tests/test_problem_repository.py` + `tests/test_contracts.py` 共 31 筆）
- 備註：於專案 `.venv` 補安裝 `ruamel.yaml`

**變更前後差異說明（選填）**：
- 前版本簡述：僅有資料契約，尚無 Problem YAML 載入層
- 後版本簡述：已具備可重用 ProblemRepository 與完整驗證測試，後續可安全銜接 Simulator

### [2026-04-22 21:34] 新增 Solver 介面、Registry 與 Config Loader
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/solver_registry.py`、`mkp/solver_config_loader.py`、`mkp/__init__.py`、`tests/test_solver_registry.py`、`tests/test_solver_config_loader.py`、`tests/test_problem_repository.py`
- 主要變更內容：
- 新增 `Solver` 協定與 `SolverRegistry`（`register/get/create`）
- 新增 `StubMaxIterationsSolver`，用於 UT-SLV 驗證 `max_iterations` 停止邏輯與 `RunResult` 契約
- 新增 `SolverConfigLoader`，使用 `ruamel.yaml` 載入並驗證 `solver_id`、`solver_class`、`stop_condition`、`params`
- 驗證 `stop_condition.type` 僅允許 `max_iterations` / `max_seconds`，並檢查對應數值需大於 0
- 補 `UT-SLV` 對應測試：註冊成功、未註冊拒絕、`RunResult` 欄位、`max_iterations` 停止行為、config schema 驗證
- 修正既有 repository 測試 fixture 縮排錯誤，確保回歸測試穩定
- 相關 Issue 或對話連結：本次「第三模組落地計畫（Solver 介面 + SolverRegistry）」
- 是否通過測試：已通過（solver + repository + contracts 共 40 筆）
- 備註：本輪仍未接舊演算法 adapter，避免與舊腳本耦合

**變更前後差異說明（選填）**：
- 前版本簡述：已有契約與 ProblemRepository，但尚無 solver 統一註冊層
- 後版本簡述：已具備 solver 介面、註冊/建立機制與 config loader，後續可銜接 Simulator

### [2026-04-22 21:41] 新增 Validator 與 ValidationReport
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/validator.py`、`mkp/__init__.py`、`tests/test_validator.py`
- 主要變更內容：
- 新增 `ValidationReport`，集中輸出可行性、objective 驗證、best_known 達標與 gap 資訊
- 新增 `Validator.validate(problem, run_result)`，完成 MKP-specific 驗證邏輯
- feasibility：逐維檢查容量違反並回傳違反維度索引
- objective：重算目標值並偵測 objective mismatch
- best_known：標記達標狀態，並計算 gap（`best_known - best_objective`）
- 新增 `UT-VAL-001~004` 對應 pytest 測試
- 相關 Issue 或對話連結：本次「第四模組落地計畫（Validator）」
- 是否通過測試：已通過（validator + solver + repository + contracts 共 44 筆）
- 備註：本輪未改動 `RunResult` 結構，避免影響既有模組

**變更前後差異說明（選填）**：
- 前版本簡述：缺少集中式 run-level 驗證邏輯
- 後版本簡述：已具備可重用 Validator 與報告結構，可供後續 ResultWriter/Summary 使用

### [2026-04-22 21:50] 新增 ResultWriter 與核心 Summary 聚合
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/result_writer.py`、`mkp/__init__.py`、`tests/test_result_writer.py`
- 主要變更內容：
- 新增 `ResultWriter` 與 `ResultEntry`
- 實作 `write_run(run_result, validation_report)`：將單次結果寫入 `runs.csv` 與 `runs.jsonl`
- 實作 `build_summary(entries)`：聚合核心統計（平均/最佳目標值、可行率、平均 runtime、平均 evaluation_count、排除原因統計）
- 實作 `write_summary(summary)`：寫入 `summary.csv` 與 `summary.json`
- 明確落實排除規則：平均/最佳目標值只納入 `is_feasible=True` 且 `objective_valid=True`
- 落實異常結果保留：infeasible/objective mismatch 仍寫入單次結果輸出
- 新增 `UT-RES-001~003` 對應測試
- 相關 Issue 或對話連結：本次「第五模組落地計畫（ResultWriter / ResultStore）」
- 是否通過測試：已通過（resultwriter + validator + solver + repository + contracts 共 47 筆）
- 備註：本輪 summary 未納入 benchmark 對照欄位（依範圍設定）

**變更前後差異說明（選填）**：
- 前版本簡述：僅有驗證層，尚無固定格式輸出與聚合
- 後版本簡述：已具備單次 run 與核心 summary 的 CSV+JSON 輸出能力

### [2026-04-22 21:56] 新增 Simulator 單次 run 流程
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/simulator.py`、`mkp/__init__.py`、`tests/test_simulator.py`
- 主要變更內容：
- 新增 `Simulator` 並實作 `run_task(task: RunTask)` 單次執行流程
- 串接既有模組：`ProblemRepository` → `SolverConfigLoader` → `SolverRegistry` → `Validator` → `ResultWriter`
- `rng` 由 `np.random.default_rng(task.seed)` 建立，確保 seed 可重現
- `run_task` 回傳 `RunResult`、`ValidationReport`、`ResultEntry`
- 新增單次流程測試：成功流程、seed 可重現、Repository 載入失敗 fail-fast、solver config 載入失敗 fail-fast
- 相關 Issue 或對話連結：本次「第六模組落地計畫（Simulator：單次 run）」
- 是否通過測試：已通過（simulator + resultwriter + validator + solver + repository + contracts 共 51 筆）
- 備註：本輪未實作批次展開與平行執行，遵循單模組邊界

**變更前後差異說明（選填）**：
- 前版本簡述：已有求解/驗證/輸出元件，但未有主流程協調者
- 後版本簡述：已具備可執行的單次 run orchestrator，可作為後續批次與 CLI 入口基礎

### [2026-04-22 22:03] 擴充 Simulator 批次展開與 fail-fast
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/simulator.py`、`tests/test_simulator_batch.py`
- 主要變更內容：
- 新增 `expand_tasks(spec)`：依 `problem_ids × solver_ids × repeat` 展開 `RunTask`
- 新增 seed 預分配：`SeedSequence(base_seed).spawn(n_tasks)`，每個 task 生成獨立可重現 seed
- 新增 `run_batch(spec)`：同步逐筆執行 `run_task`，收集成功結果列表
- 落實 fail-fast：任一 task 發生錯誤立即中止並向上拋出
- 新增批次測試：task 展開、seed 可重現與獨立、problem/config 錯誤 fail-fast
- 相關 Issue 或對話連結：本次「第七模組落地計畫（Simulator：批次展開 + fail-fast）」
- 是否通過測試：已通過（simulator_batch + 既有全回歸共 55 筆）
- 備註：本輪仍未實作平行執行，符合單模組邊界

**變更前後差異說明（選填）**：
- 前版本簡述：僅有單次 `run_task`，無批次任務展開
- 後版本簡述：已具備同步批次展開與 fail-fast 中止策略，可支援完整批次實驗流程

### [2026-04-22 22:15] 新增 Command/App 並打通 CLI 到 run_batch
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/app.py`、`mkp/__init__.py`、`tests/test_app.py`
- 主要變更內容：
- 新增 `build_parser()` 與 flat args 入口
- 新增 `create_experiment_spec()`，將 CLI 參數轉為 `ExperimentSpec`
- 新增 `preflight_validate()`：檢查 problem/solver yaml 是否存在，缺檔直接 fail-fast
- 新增 `build_simulator()`：組裝 Repository/Registry/ConfigLoader/Validator/ResultWriter/Simulator
- 新增 `main()`：正式打通 `CLI -> ExperimentSpec -> Simulator.run_batch`
- 新增 app 測試：合法執行、problem 缺檔、solver 缺檔、必要參數缺失
- 相關 Issue 或對話連結：本次「第八模組落地計畫（Command/App：CLI 到 run_batch）」
- 是否通過測試：已通過（app + 既有全回歸共 59 筆）
- 備註：本輪保留 stub solver 註冊，方便後續替換成真實 solver adapter

**變更前後差異說明（選填）**：
- 前版本簡述：系統模組已齊但缺正式 CLI 入口
- 後版本簡述：已具備可直接命令列執行的最小完整流程，第一階段主幹路徑基本打通

### [2026-04-22 22:20] 接入 BSMA 真實 solver 與重現性驗證
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_v1_008_solver.py`、`mkp/app.py`、`mkp/__init__.py`、`mkp/configs/solvers/bsma_v1_008.yaml`、`tests/test_bsma_solver.py`
- 主要變更內容：
- 新增 `BSMAV1008Solver`，提供 `solve(problem, config, rng) -> RunResult`
- 將 solver 亂數完全改為使用傳入 `numpy.random.Generator`，移除對全域 `random/np.random.seed` 的依賴
- 在 app 組裝層註冊 `bsma_v1_008`，保留 `stub_solver` 以利比較與回退
- 新增 `bsma_v1_008` solver yaml 設定檔（stop_condition + params）
- 新增真實 solver 測試：registry 建立、合法輸出、同 seed 重現、不同 seed 隔離、stop_condition 行為
- 相關 Issue 或對話連結：本次「第九模組落地計畫（接入真實 heuristic + 重現性）」
- 是否通過測試：已通過（bsma + app + simulator + 既有回歸共 64 筆）
- 備註：本輪未修改 `old/*.py`，避免污染舊基線

**變更前後差異說明（選填）**：
- 前版本簡述：僅有 stub solver，可測流程但無真實 heuristic
- 後版本簡述：已可透過正式流程執行第一個真實 heuristic，並具備重現性保證

### [2026-04-22 22:32] 新增第一階段整體驗證 runner（baseline/refactor 對照）
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/stage1_validation.py`、`mkp/__init__.py`、`tests/test_stage1_validation.py`
- 主要變更內容：
- 新增 `ValidationScenario` 與固定 smoke 清單（`WEISH/weish01` + 3 seeds）
- 新增 refactor 執行與同 seed 重跑一致性檢查
- 新增 old script 執行與 baseline objective 收集、差異分類（`match`/`objective_mismatch`/`baseline_missing`）
- 新增 `comparison.json` 與 `summary.txt` 報告輸出
- 新增 dat→yaml 自動轉換流程，補齊 stage1 驗證所需 `mkp/configs/problems/...` 輸入
- 新增對應 pytest 測試，覆蓋清單、重現檢查、差異分類、dat 轉 yaml
- 相關 Issue 或對話連結：本次「第一階段整體驗證與收斂計畫」
- 是否通過測試：已通過（`tests/test_stage1_validation.py` + 全回歸 `68 passed`）
- 備註：實跑產出目錄為 `mkp/output/stage1_validation/20260422_223243`

**變更前後差異說明（選填）**：
- 前版本簡述：系統可執行 batch，但缺少新舊輸出對照與收斂報告流程
- 後版本簡述：已具備第一階段 smoke 驗證 runner，可執行 refactor 基線檢查並輸出比較報告

### [2026-04-22 22:43] 補齊 old 依賴並讓 baseline 比對生效
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/stage1_validation.py`
- 主要變更內容：
- 在專案 `.venv` 安裝 `pandas`、`scipy` 以滿足 `old/main1weish.py` 與 legacy solver 相依
- 調整 `run_old_script` 相容執行流程：加入 legacy 模組 alias（`BSMA -> BSMA2`、`BSCASMA2 -> BSCASMA`）
- 加入 `data\\WEISH` 路徑相容處理，避免舊腳本在 Linux 下初始化失敗
- 加入空 `algorithm_select` fallback，確保 baseline CSV 可產出並被 comparison 流程解析
- 相關 Issue 或對話連結：本次「補齊 old 依賴並重跑驗證」
- 是否通過測試：已通過（`tests/test_stage1_validation.py` + 全回歸 `68 passed`）
- 備註：stage1 實跑輸出 `mkp/output/stage1_validation/20260422_224335`，`baseline_match=3`

**變更前後差異說明（選填）**：
- 前版本簡述：old baseline 無法形成，comparison 全為 `baseline_missing`
- 後版本簡述：old baseline 可形成並可解析，comparison 進入有效對照（本輪 `match`）

### [2026-04-22 22:47] 新增 BSMA 嚴格等價驗證器（不修改 old）
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_equivalence.py`、`mkp/__init__.py`
- 主要變更內容：
- 新增 `verify_equivalence()` 與 `write_equivalence_report()`，對 old/new 在同 seed 下執行逐步 trace 比對
- trace 比對維度包含每步 `sort`/`repair` 的 `digest` 與 `best_fit`，可檢出第一個偏離點
- 輸出報告檔：`mkp/output/stage1_validation/bsma_equivalence_report.json`
- 匯出 `EquivalenceReport`、`verify_equivalence`、`write_equivalence_report` 供外部呼叫
- 相關 Issue 或對話連結：本次「BSMA 嚴格等價驗證（中間過程 + 最終結果）」
- 是否通過測試：已通過（`tests/test_bsma_solver.py`）
- 備註：本次實跑驗證結果為不等價（trace 與 final objective 皆不一致）

**變更前後差異說明（選填）**：
- 前版本簡述：僅能比較最終輸出，不足以判定中間過程一致性
- 後版本簡述：可直接做嚴格等價驗證，並精確定位 first mismatch 步驟

### [2026-04-22 23:02] BSMA 對齊：改為 legacy adapter 並通過嚴格等價
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_v1_008_solver.py`、`mkp/bsma_equivalence.py`
- 主要變更內容：
- 將 `BSMAV1008Solver` 重構為 legacy adapter，直接調用 `old/BSMA2.py::BSMA_V1_008`（不改 old 原檔）
- 新增 `run_seed` 支援，確保嚴格驗證時 old/new 可使用同一 seed
- 將嚴格等價驗證器的新路徑改為對 adapter 執行 trace 比對，確保比較口徑一致
- 實跑報告：`mkp/output/stage1_validation/bsma_equivalence_report.json`
- 本次結果：`final_objective_old=4554`、`final_objective_new=4554`、`final_solution_match=true`、`trace_match=true`
- 相關 Issue 或對話連結：本次「BSMA 對齊模式」
- 是否通過測試：已通過（`tests/test_bsma_solver.py`、全回歸 `68 passed`）
- 備註：驗證案例為 `WEISH/weish01/seed=101/max_iter=30`

**變更前後差異說明（選填）**：
- 前版本簡述：有嚴格驗證能力，但 solver 與 old 在 trace 層仍存在偏移
- 後版本簡述：solver 執行路徑已對齊 legacy，嚴格驗證在指定案例達成完全一致

### [2026-04-23] BSMA：改為內建 Core + equivalence 只掛 Core
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_v1_008_solver.py`、`mkp/bsma_equivalence.py`、`mkp/__init__.py`、`tests/test_bsma_solver.py`
- 新增檔案：`tests/test_bsma_equivalence.py`
- 主要變更內容：
- 新增 `BSMAV1008Core`，邏輯與 `old/BSMA2.py::BSMA_V1_008` 對齊且不 `import old`
- `BSMAV1008Solver.solve` 改為建立 `BSMAV1008Core` 並呼叫 `run()`；`run_seed` 仍可由 config 覆寫
- `bsma_equivalence` 的 new 路徑改為僅暫時替換 `BSMAV1008Core.repair/sort_pop`，確保與 solver 為同一執行本體
- 調整 BSMA 單元測試：移除與舊版不符之可變 `pop_size`，改以固定族群大小 20 驗證 `evaluation_count`
- 相關 Issue 或對話連結：本次「equivalence 與 solver 一致後再對 old」
- 是否通過測試：已通過（全回歸 `69 passed`，含 `tests/test_bsma_equivalence.py`）
- 備註：`WEISH/weish01/seed=101/max_iter=30` 嚴格 trace 仍為一致

**變更前後差異說明（選填）**：
- 前版本簡述：solver 直接委派 old，驗證 new 路徑 patch 的是 `BSMA2` 類別，與「mkp 內實作」口徑不一致
- 後版本簡述：solver 與 equivalence 的 new 端共用 `BSMAV1008Core`，對 old 的比對仍維持獨立讀取 `old/BSMA2`

### [2026-04-23] BSMA Debug：外層迭代摘要與三 seeds 驗收
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_v1_008_solver.py`、`mkp/bsma_equivalence.py`、`tests/test_bsma_equivalence.py`
- 主要變更內容：
- `BSMAV1008Core` 新增可選 `_loop_trace`：每外層迭代記錄 `a`、`b`、`W0_digest`、`i0` 分支、`p_i0` 或 `vb0_digest`/`vc0_digest`、`best_after_sort`
- `verify_equivalence` / `write_equivalence_report` 新增 `include_loop_meta`，回傳 `loop_meta_new` 供 mismatch 診斷
- 新增測試：`WEISH/weish01` 三 seeds（101/202/303）嚴格 trace；`include_loop_meta` 形狀檢查（支援提前達 best_known 而迭代數少於 max_iter）
- `EquivalenceReport` 改為非 frozen 以容納 `loop_meta_new` 列表
- 相關 Issue 或對話連結：本次「BSMA 對齊 Debug 計畫」
- 是否通過測試：已通過（全回歸 `71 passed`）

**變更前後差異說明（選填）**：
- 前版本簡述：僅有 repair/sort trace，難以區分迭代內 RNG 與公式差異
- 後版本簡述：可選擇性輸出每外層迭代摘要，並以三 seeds 固定驗收擴大信心

### [2026-04-23 15:30] WEISH 串流等價驗證、批次 runner 與 pytest slow
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_equivalence.py`、`mkp/bsma_equivalence_batch.py`、`pytest.ini`、`tests/test_bsma_weish_suite_smoke.py`、`tests/test_bsma_weish_suite_slow.py`
- 主要變更內容：
- `verify_equivalence_streaming`：new 端以檔案 `readline` 串流比對 expected digest；`repair`/`sort` 的 `step` 計數與 `_run_new_with_trace` 一致
- `EquivalenceReport` 新增 `new_stop_reason`、`new_reported_eval_count`（串流路徑填入；記憶體路徑為 `None`）
- 新增 `run_weish_equivalence_suite` 與 CLI `python -m mkp.bsma_equivalence_batch`（輸出 `mkp/output/bsma_weish_suite/`）
- `pytest.ini` 註冊 `slow`；smoke 測試小 budget；`test_bsma_weish_suite_slow` 全 30 題 × 10 萬 budget
- 相關 Issue 或對話連結：見「對話紀錄.md」2026-04-23 WEISH 全題庫條目
- 是否通過測試：已通過（`pytest tests/ -m "not slow"` → 73 passed, 1 deselected）
- 備註：`problem_dat_converter` 與 `stage1_validation` 委派為前序對話已完成之共用載入

**變更前後差異說明（選填）**：
- 前版本簡述：大迭代僅能記憶體累積 trace，難以跑滿 30 題 × 高迭代
- 後版本簡述：可串流驗證並有批次摘要與可選全量 slow 測試

### [2026-04-23 16:25] pytest 子目錄收集與 pytest11 插件
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/pytest_plugin.py`、`pyproject.toml`、`pytest.ini`（註解）
- 主要變更內容：pytest 僅在 `invocation_dir == rootpath` 時套用 `testpaths`；於 `pytest11` 註冊 `pytest_load_initial_conftests`，在從子目錄啟動且未指定路徑時插入 `<rootdir>/tests`。
- 相關 Issue 或對話連結：於 `~/mkp/mkp` 執行 `pytest -m slow` 得到 0 tests、`python -m mkp` 找不到套件（後者需 `pip install -e .`）。
- 是否通過測試：已通過（自 `mkp/mkp`：`pytest -m slow --collect-only`；`pytest -m \"not slow\"` 73 passed）
- 備註：安裝後需執行 `pip install -e .` 以註冊 pytest 插件與可編輯套件

### [2026-04-23 16:05] 修正 bsma_equivalence_batch 預設 repo_root
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_equivalence_batch.py`
- 主要變更內容：CLI 未指定 `--repo-root` 時，由 `Path(__file__).parents[2]` 改為 `parents[1]`，使預設指向含 `data/WEISH` 的專案根（`/.../mkp`），避免誤用使用者家目錄。
- 相關 Issue 或對話連結：使用者執行 `python -m mkp.bsma_equivalence_batch` 出現 `Missing dat file: /home/sean/data/WEISH/...`
- 是否通過測試：已手動確認 `dat_file_path` 指向 `.../mkp/data/WEISH/weish01.dat` 且檔案存在
- 備註：仍可用 `--repo-root` 覆寫
