### [2026-05-03] BSCASMA Numba：混合段 RNG／dtype 與 `sort_pop` float 族群
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`solver/BSCASMA.py`、`solver/BSCASMA_numby.py`（既有）、`log/對話紀錄.md`
- 主要變更內容：`update_sma_weight` 與 `BSMA_numby` 一致（向量 `np.random.random(items)`、`i < pop_size / 2`）；`sma_local` 向量 `uniform` 與每維重填 `pool`；`sca_*` 使用 `math.pi`／`math.sin`／`math.cos`；`sort_pop`／`individual_best_sol` 改 float64 以避免 SCA 中間值寫入 int 陣列被截斷、與 njit 分叉；模組 docstring 補充混合對齊說明。
- 相關 Issue 或對話連結：`log/對話紀錄.md` 同日「BSCASMA Numba 對齊」
- 是否通過測試：`pytest tests/test_bscasma_numba_gk.py tests/test_bscasma_equivalence.py tests/test_bscasma_solver.py` 全數通過
- 備註：`brlsmasca2_numba` 註冊與測試檔於先前回合已加入；本回合僅修行為對齊。

### [2026-05-03] 新增 BSCA2 Numba 版與 GK／WEISH 對齊測試
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`solver/BSCA2_numby.py`（新增）、`configs/solvers/bsca2_numba.yaml`（新增）、`tests/test_bsca2_numba_gk.py`（新增）、`tests/test_bsca2_equivalence.py`、`engine/builders.py`、`simulator/core.py`
- 主要變更內容：`BSCA2V120NumbaCore` 與 `BSCA2V120NumbaSolver`（`bsca2_numba`）；`_repair_bsca2_row_inplace` 第二段掃完 `cp_list` 不提前 `break`；`tests/test_bsca2_equivalence.py` 改為 `BSCA2V120Core` 與 `BSCA2V120NumbaCore` 在 weish01 上比對（取代與 `old` 的逐代 bit-identical，因參考版已改單一 RNG／決定性排序）。
- 相關 Issue 或對話連結：`log/對話紀錄.md` 同日「BSCA2 Numba 版與測試」
- 是否通過測試：`pytest tests/test_bsca2_equivalence.py tests/test_bsca2_solver.py tests/test_bsca2_numba_gk.py` 全數通過
- 備註：`valid/bsca2_population_trace.py` 仍以 old 為對照；若需舊行為請手動跑該模組。

### [2026-05-03] BSMA／BSMA_numby：Gbest 初始化 deepcopy 與單一 njit 主迴圈對齊
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`solver/BSMA.py`、`solver/BSMA_numby.py`
- 主要變更內容：`BSMACore.run` 在首次 `sort_pop` 後改為 `deepcopy(self.pop_sol[0])`／`deepcopy(self.pop_fit[0])` 作為 `Gbest_sol`／`Gbest_fit`，避免與 `pop_sol[0]` 共用視圖導致 `i==0` 局部搜尋時 `Gbest_sol[j]` 隨列即時變動，與 Numba 版獨立 `gbest_sol` 緩衝語意不一致；`pseudo_utility` 回傳與 Numba 一致之 `int64` contiguous `argsort`；`BSMA_numby` 以單一 `_bsma_main_loop_numba`（內含決定性排序）取代每代進出 njit，並維持不使用 `objmode`。
- 是否通過測試：`pytest tests/test_bsma_numba_gk.py -q` 全數通過

### [2026-05-03] pseudo_utility：抑制 linprog 退化時除零 RuntimeWarning
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`solver/BSMA.py`、`solver/BSMA_numby.py`、`solver/BSCA2.py`、`solver/BSCASMA.py`、`solver/adapters/mkp_sma_adapter.py`
- 主要變更內容：`pseudo_utilities` 除法外層使用 `np.errstate(divide="ignore", invalid="ignore")`，與舊版公式一致、不改數值，僅避免大量 `RuntimeWarning`。
- 是否通過測試：未跑 pytest

### [2026-05-03] MKPProblem 統一 int64 C-contiguous；solver 與 BSMANumbaCore 信任契約
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`engine/models.py`（`_as_int_array` / `_as_int_matrix`、`MKPProblem` 說明、`TSPProblem` 距離矩陣）、`solver/BSMA.py`、`solver/BSCA2.py`、`solver/BSCASMA.py`、`solver/BSMA_numby.py`
- 主要變更內容：`MKPProblem` 陣列於載入時即 `np.int64` 且 C-contiguous；TSP `distance_matrix` 同；`BSMA`/`BSCA2`/`BSCASMA` 的 `solve` 直接傳 `problem.values` 等；`BSMANumbaCore` 以 `_expect_mkp_problem_tensors` 驗證契約、移除重複 `ascontiguousarray`，`pseudo_utility` 回傳明確 `int64` 連續 `cp_list`。
- 相關 Issue 或對話連結：使用者要求資料路徑在解析端固定格式供 Numba
- 是否通過測試：`compileall` 通過；未跑 pytest
- 備註：直接建構 `BSMANumbaCore` 且未經 `ProblemModel` 者須自行提供 int64 連續陣列。

### [2026-05-03] engine.build 僅驗證實驗題目；stderr 啟動階段訊息
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`engine/bank.py`、`engine/assembly.py`、`tests/test_engine.py`
- 主要變更內容：新增 `validate_spec_problems_in_repository`；`build()` 改為 `assert_spec_problems_in_catalog` 後只對 spec 題目 `load`；`validate_catalog_entries` 保留並加 docstring 說明用途；`build()` 對 stderr 印掃描／載入題目／solver／SHM 階段；測試改名並改 `problem_ids` 含 `broken` 才預期失敗，新增未使用壞檔時 build 成功測試。
- 相關 Issue 或對話連結：`log/對話紀錄.md` 同日條目
- 是否通過測試：`python3 -m compileall` 通過；本機無 pytest 未執行
- 備註：全庫 YAML 檢查請改呼叫 `validate_catalog_entries` 或獨立 CLI。

### [2026-05-03] Core 建構子納入 max_iter；移除 5000 魔數與事後覆寫
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`solver/BSMA.py`、`solver/BSCA2.py`、`solver/BSCASMA.py`、`valid/bsma_equivalence.py`、`valid/bsma_population_trace.py`、`valid/bsca2_population_trace.py`、`valid/bscasma_population_trace.py`
- 主要變更內容：`BSMAV1008Core`／`BSCA2V120Core`／`BRLSMASCA2V100320050TestCore` 以 keyword-only 必填 `max_iter`（及 BSMA／BSCASMA 的 `pop_size`、`z`；BSCA2 的 `pop_size`、`a`）；刪除 `self.max_iter = 5000` 與 Solver 端 `core.max_iter = ...`；`valid` 內 new trace 路徑改於建構子傳入 `max_iter`。
- 相關 Issue 或對話連結：`log/對話紀錄.md` 2026-05-03 條目
- 是否通過測試：本環境 `python3 -m compileall` 通過；未執行 `pytest`（環境無 pytest）
- 備註：直接實例化 Core 的程式必須補齊新參數。

**變更前後差異說明**：
- 前版本簡述：Core `__init__` 內寫死 `max_iter=5000`，Solver 在 `run()` 前覆寫。
- 後版本簡述：迭代上限與超參數一律由建構子明確傳入，無隱藏魔數。

### [2026-05-02 20:00] 新增 BSCA2_V1_20 與 BRLSMASCA2_V1_003_20_050_test 兩個 solver；補對應 trace/測試
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：
  - 新增：`solver/BSCA2.py`（`BSCA2V120Core` + `BSCA2V120Solver`）；`solver/BSCASMA.py`（`BRLSMASCA2V100320050TestCore` + `BRLSMASCA2V100320050TestSolver`）
  - 新增：`configs/solvers/bsca2_v1_20.yaml`、`configs/solvers/brlsmasca2_v1_003_20_050_test.yaml`
  - 新增：`valid/bsca2_population_trace.py`、`valid/bscasma_population_trace.py`（仿 `valid/bsma_population_trace.py` 的逐代族群快照 bit-identical 對比；CLI `--max-iterations`、`--seed`、`--problems`、BSCASMA 額外有 `--a / --z / --prob-arr`）
  - 新增：`tests/test_bsca2_solver.py`、`tests/test_bscasma_solver.py`（registry / SolveResult / reproducibility / stop_reason / evaluation_count / params 驗證；BSCASMA 因 old `S=0` NaN bug 改用 `ProblemRepository` 載入 WEISH/weish01 真題作 unit test 基底）
  - 新增：`tests/test_bsca2_equivalence.py`、`tests/test_bscasma_equivalence.py`（weish01 + 3 個 seeds + max_iter=30 的 bit-identical 回歸錨點；41.92s 全綠）
  - 修改：`engine/builders.py`（`default_solver_builders` 加入 `bsca2_v1_20` 與 `brlsmasca2_v1_003_20_050_test` 兩個 lambda）
  - 修改：`simulator/core.py::_PROCESS_SAFE_SOLVERS`（加入兩個新 id）
  - 修改：`__init__.py`（exports / `__all__` 加入 `BSCA2V120Core`、`BSCA2V120Solver`、`BRLSMASCA2V100320050TestCore`、`BRLSMASCA2V100320050TestSolver`）
  - 修正 bug：`cli/run/main.py::main`（補回 `argv: list[str] | None = None, *, problem_root, solver_root` kwargs；上一輪 cli/run 重構後 `tests/test_app.py` 5 個測試一直 fail，本次連帶修復）
- 主要變更內容：
  - 兩個 Core 完全保留 old 行為（dead code、未使用屬性、可疑公式、`S=0` NaN bug 等），只移除 old 在 run() 內每 500 / 50 代的 print；雙源 RNG（`random` + `np.random`）以 sandwich 方式 seed（Solver 端建構前一次、Core.run() 開頭再一次），與 BSMA 那次處理一致。
  - BSCA2 在 i 迴圈內每次 sort_pop 並判斷 Gbest；BSCASMA test 版有 `init_best_method` 在 `__init__` 吃 `np.random.choice`、`policy()` 90/10 規則、`update_sma_weight` 對 `S=0` 不保護等三大不同點。
  - 兩個 trace 模組產出 `output/<name>_population_trace/{traces/*.csv, summary.csv, summary.json}`；CLI `python -m mkp.valid.bsca2_population_trace` / `python -m mkp.valid.bscasma_population_trace` 預設 12 題（HP/PB/PET/SENT/WEING/WEISH × 2）× seed=101 × max_iter=5000；exit code 0/1 對應 all_pass。
- 相關 Issue 或對話連結：`log/對話紀錄.md` 2026-05-02 20:00 條目；計畫檔 `bsca2_bscasma_solver_*`
- 是否通過測試：
  - `pytest tests/ -m "not slow" -q` → 130 passed（含 14 個新 solver test、4 個新 equivalence test，並修復 cli/run main 後讓 5 個 `tests/test_app.py` 重綠）
  - `tests/test_bsca2_equivalence.py` + `tests/test_bscasma_equivalence.py` 共 4 testcase 41.92s 全綠
  - 完整 12 題 × 5000 iter 等價驗證：
    - `python -m mkp.valid.bsca2_population_trace` → all_pass=true (12/12)，耗時 2427.565s，summary 在 `output/bsca2_population_trace/`
    - `python -m mkp.valid.bscasma_population_trace` → all_pass=true (12/12)，耗時 814.178s，summary 在 `output/bscasma_population_trace/`；其中 PB/pb2 (iter 585)、SENT/sent01 (iter 16)、WEING/weing2 (iter 14)、WEISH/weish02 (iter 6) 都觸發 old NaN bug，但 old 與 new 在同一 iter、同型例外崩潰（crash_match=True），仍算 bit-identical
- 備註：
  - BSCASMA `update_sma_weight` 的 `S=0` 不保護是 old/BSCASMA.py 第 631 行已知 bug（註解說要加 1e-8 但實作沒加）；fidelity 要求保留，故 unit test 必須避開觸發。
  - 為支援上述「兩端同 iter 同例外崩潰也算 bit-identical」，`valid/bscasma_population_trace.py` 新增 `_safe_run_with_trace` 與 `TraceRunResult.crashed_iteration / crashed_exception`、`ProblemTraceSummary.old_crashed_iteration / new_crashed_iteration / crash_match`、`SUMMARY_FIELDNAMES` 增 3 欄；status 印出時若有任一端崩潰會附 `[crashed old@N new@M crash_match=...]`。

**變更前後差異說明**：
- 前版本簡述：`solver/` 只有 BSMA；`engine/builders.py` 只註冊 `stub_solver` 與 `bsma_v1_008`；`tests/test_app.py` 5 個 testcase fail（main 缺 kwargs）。
- 後版本簡述：`solver/` 多兩套 Core+Solver；註冊 4 個 solver；`tests/test_app.py` 全綠；新增兩個 valid trace 模組與 4 個等價測試作為「old vs new bit-identical」的回歸錨點。

### [2026-05-02] Simulator 拆出 ResultWriter；新增 stat 模組；RunResult → SolveResult；修 cli/run 既有 bug
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：
  - 新增：`tools/stat.py`（`ResultEntry`、`write_simulator_result`、`build_summary`、內部 `_build_entry/_entry_to_dict/_excluded_counts/_write_runs_csv/_write_runs_jsonl/_write_summary`），`tests/test_stat.py`
  - 重構：`simulator/core.py`（加入 `SimulatorRunRow` / `SimulatorResult`；`Simulator.__init__` 移除 `result_writer`；`run_task` 改回傳 `SimulatorRunRow`；`run_sequential` / `run_batch` 改回傳 `SimulatorResult`；刪除 `_finalize_batch`；`_build_rows_from_run_results` 改名為 `_build_rows_from_solve_results`），`simulator/__init__.py` 暴露新型別
  - 重構：`engine/assembly.py::SimulationBundle.NewSimulatorWithSeed`（不再建立 `ResultWriter`）
  - 重構：`cli/run/support.py::executeSimulator`（在 sim 跑完後呼叫 `write_simulator_result`；額外接受 `problem_root` / `solver_root` 可注入；`_validate` 改名公開為 `validate_execute_args`；修正 `createExperimentSpec` 的 `tuple(solver)` 把字串拆字元的 bug → 改為 `(solver,)`）
  - 修正 bug：`cli/run/main.py`（`args.outputDir` → `args.output_dir`；`main(argv=None, *, problem_root, solver_root)` 對齊端到端測試）
  - 修正 bug：`cli/run/__init__.py`（移除不存在的 `build_parser` / `create_experiment_spec` 引用，改 export 真實存在的 `parser` / `createExperimentSpec` / `validate_execute_args`）
  - 改名：`engine/models.py`（`RunResult` → `SolveResult`），全 repo import 與型別標註同步（`solver/BSMA.py`、`solver/registry.py`、`solver/validator.py`、`simulator/core.py`、`__init__.py`、四份相關測試）；`Validator.validate` 參數名改為 `solve_result`
  - 刪除：`tools/result_writer.py`（功能整批搬到 `tools/stat.py`，不留 shim）
  - 測試：`tests/test_result_writer.py` 重命名為 `tests/test_stat.py` 並切換到新 API；`tests/test_simulator.py`、`tests/test_simulator_batch.py` 之 `_build_simulator` 不再注入 `result_writer`，並補上 `write_simulator_result` 呼叫保留檔案存在性覆蓋率；`tests/test_engine.py::test_engine_build_returns_bundle_with_runnable_simulator` 補 `write_simulator_result` 呼叫；`tests/test_app.py` 對齊新 import；`tests/test_validator.py` / `tests/test_contracts.py` 純改名
- 主要變更內容：`Simulator` 從「邊跑邊寫」改成「跑完一次性提交」；輸出職責由 `mkp.tools.stat.write_simulator_result` 負責；`RunResult` 改名為 `SolveResult`。
- 相關 Issue 或對話連結：`log/對話紀錄.md` 2026-05-02 條目；計畫檔 `stat_module_simulator_result_*`
- 是否通過測試：`pytest tests/ --ignore=tests/test_bsma_weish_suite_slow.py -q` → 112 passed；端到端 CLI smoke（最小 `configs/` 環境執行 `python -m mkp.cli.run --experiment-id smoke --dataset WEISH --problems weish01 --solver bsma_v1_008 --repeat 1 --base-seed 1`）成功產出 `runs.csv` / `runs.jsonl` / `summary.json` / `summary.csv`，欄位與重構前完全一致
- 備註：行為變化——`runs.csv` / `runs.jsonl` 不再 streaming append，跑到一半 crash 將失去全部結果（已與使用者確認）

**變更前後差異說明**：
- 前版本簡述：`Simulator` 持有 `ResultWriter`，每筆 `run_task` 即 append 寫入 csv/jsonl；批次結束 `_finalize_batch` 一次寫 summary。`run_task` 回傳 `tuple[RunResult, ValidationReport, ResultEntry]`，`run_sequential` / `run_batch` 回傳 `list[tuple[...]]`。
- 後版本簡述：`Simulator` 只負責執行與驗證，回傳 `SimulatorResult(rows=tuple[SimulatorRunRow])`；輸出檔由 `executeSimulator` 在批次結束後呼叫 `write_simulator_result` 一次性寫出全部 4 個檔案。`RunResult` 已改名為 `SolveResult`。

### [2026-05-01] CLI run：`preflight_validate` 併入 `validate_execute_args`；`main`／`build_parser` 修復
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`cli/run/support.py`（合併驗證邏輯、`build_parser = parser` 別名）、`cli/run/main.py`（`main(argv=None, *, problem_root, solver_root)`、`build_parser`）、`cli/run/__init__.py`（export `build_parser`、移除 `preflight_validate`）
- 主要變更內容：執行前檢查不再分兩層；對外無 `preflight_validate`。
- 相關 Issue 或對話連結：`log/對話紀錄.md` 2026-05-01 條目
- 是否通過測試：此環境未執行 pytest；請於本機跑 `pytest tests/test_app.py` 或全量 `pytest tests/ -m "not slow"`
- 備註：無

### [2026-04-26] BSMA：`pop_size`／`z` 由 yaml `params` 注入 Core；等價與測試
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`solver/bsma_v1_008_solver.py`（前序／本輪：`solve` 讀 `params`、`Core` 建構子）、`valid/bsma_equivalence.py`（顯式 `pop_size=20`、`z=0.08`）、`tests/test_bsma_solver.py`（`test_bsma_params_pop_size_from_config_affects_evaluation_count`）
- 主要變更內容：正式求解路徑使用快照內 config 的 `params.pop_size`、`params.z`（與 `bsma_v1_008.yaml` 一致）；等價 capture 與 `old/BSMA2` 對齊仍用 20／0.08
- 是否通過測試：`pytest tests/ -m "not slow"` 93 passed

### [2026-04-27] BSMA：`random` 改為 `np.random`（對照 old/BSMA2 同步）
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`solver/bsma_v1_008_solver.py`、`old/BSMA2.py`、`valid/bsma_equivalence.py`（移除 `random.seed`／`import random`）
- 主要變更內容：`random.uniform(0,1)` 改 `np.random.uniform(0.0, 1.0)`；僅以 `np.random.seed` 固定種子，維持等價驗證 trace
- 是否通過測試：`pytest tests/ -m "not slow"` 92 passed

### [2026-04-27 01:25] RunResult／輸出新增 `linprog_runtime`；BSMA 計時
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`engine/contracts.py`（`RunResult.linprog_runtime`）、`solver/bsma_v1_008_solver.py`（`perf_counter` 包住 `linprog` 與整段 `Core`+`run`）、`solver/solver_registry.py`、`tools/result_writer.py`、`tests/test_*`
- 主要變更內容：`runtime` 在 BSMA 為整體演算法秒數；`linprog_runtime` 僅 LP；`runs.csv`／`runs.jsonl` 多一欄
- 是否通過測試：`pytest tests/ -m "not slow"` 92 passed

### [2026-04-27 01:12] 預載 solver 設定：`SolverConfigsSnapshot` 與 Bundle／Simulator
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`engine/solver_configs_snapshot.py`（新增）、`engine/bundle.py`、`engine/__init__.py`、`simulator/core.py`、`cli/run/support.py`、`__init__.py`、`tests/test_simulator.py`、`tests/test_simulator_batch.py`、`tests/test_engine.py`
- 主要變更內容：`engine.build` 以 `SolverConfigLoader` 載入 `spec.solver_ids` 後深拷貝封入 `SimulationBundle.solver_configs`；`Simulator` 改接快照，`run_task`／worker 僅 `get`／dict；`run_batch` 恢復 guard 並以 `to_worker_init_dict` pickle；solver 失敗早於 SHM 建立；CLI 僅 `worker_curriculum`+`repeat>1` 呼叫 `run_batch`
- 是否通過測試：`pytest tests/ -m "not slow"` 91 passed
- 備註：`test_engine` 改為呼叫 `NewSimulatorWithSeed`

### [2026-04-27 00:36] RunResult 移除 repeat_index；write_run 顯式帶 repeat
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`engine/contracts.py`、`tools/result_writer.py`、`simulator/core.py`、`solver/solver_registry.py`、`solver/bsma_v1_008_solver.py`、`tests/test_*`（contracts／result_writer／simulator／validator／batch）
- 主要變更內容：`RunResult` 不再含 `repeat_index`；`write_run`／`_build_entry` 以關鍵字參數 `repeat_index` 寫入 `ResultEntry`／CSV／JSONL；`_build_rows_from_run_results` 改為 `(tasks, lines, per_line_results)` 並以 `zip(lines, chunks)` 建 `(problem,solver,repeat)→RunResult` 再依 `expand_tasks` 順序輸出
- 是否通過測試：`pytest tests/ -m "not slow"` 90 passed
- 備註：子行程回傳之 `RunResult.seed` 未必等於 `RunTask.seed`，故不可用 seed 做 join

### [2026-04-27 00:11] `run_batch` process-only 後調整併發測試
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`tests/test_simulator_batch.py`（`test_run_batch_worker_curriculum_uses_process_pool`：改 `stub_solver`、移除 Barrier／`threading`）
- 主要變更內容：與 `_PROCESS_SAFE_SOLVERS` 對齊；子行程仍由 `simulator.core` 內建 registry 使用 `StubMaxIterationsSolver`；父行程 registry 維持 `CountingSolver` 與其他測試一致
- 相關 Issue 或對話連結：見 `log/對話紀錄.md` 同日條目
- 是否通過測試：`pytest tests/ -m "not slow"` 91 passed
- 備註：`simulator/core.py` 之 process-only 與 `RuntimeError` 為前序對話已合併之邏輯

### [2026-04-27] `run_sequential`／`executeSimulator` 分派；`run_batch` 僅 worker_curriculum 併發
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`simulator/core.py`（新增 `run_sequential`、`_finalize_batch`；`run_batch` 移除 grid 串行分支並對非 worker 模式 raise）、`cli/run/support.py`（`executeSimulator`：`repeat==1` 或 grid 走 `run_sequential`，`worker_curriculum` 且 `repeat>1` 走 `run_batch`）、`engine/bundle.py` 註解、`tests/test_engine.py`、`tests/test_simulator_batch.py`
- 是否通過測試：`pytest tests/ -m "not slow"` 91 passed

### [2026-04-27] 目錄重組後 canonical 匯入（`engine` / `tools` / `solver` / `valid`）
**提交人**：AI 協作
**更新摘要**：
- 新增：`tools/__init__.py`、`solver/__init__.py`、`valid/__init__.py`；`pyproject.toml` 明列 `mkp.tools`、`mkp.solver`、`mkp.valid`
- 主要變更內容：`engine/bundle`／`simulator/core`／`engine/defaults`／`cli/run`／`converter`／`valid/*`／`tools/result_writer`／`solver/*` 相對匯入對齊新位置；根 `__init__.py` 自子套件 re-export；`tests/*` 改為 `mkp.engine.*`、`mkp.tools.*`、`mkp.solver.*`、`mkp.valid.*`
- 是否通過測試：`pytest tests/ -m "not slow"` 91 passed；`python -m mkp.cli.run --help` 與 weish01+bsma_v1_008 煙霧執行成功
- 備註：Canonical 匯入見計畫文件；`python -m mkp` 需根目錄 `__main__.py`（本輪未加）

### [2026-04-26] `simulator` 子套件化（`simulator/core.py` + `__init__.py`）
**提交人**：AI 協作
**更新摘要**：
- 刪除：根目錄 `simulator.py`
- 新增：`simulator/__init__.py`（匯出 `Simulator`）、`simulator/core.py`（原實作，相對匯入改為自父套件 `..` 起算）
- 修改：`pyproject.toml` 納入 `mkp.simulator`；`note/實作規劃.md` 路徑敘述
- 匯入方式不變：`from mkp.simulator import Simulator`
- 是否通過測試：`pytest tests/ -m "not slow"` 91 passed
- 其他：`bsma_v1_008_solver.py`、`validator.py` 需位於套件根（歷程中曾短暫置於 `solver/`，已移回以維持 `mkp.*` 匯入）

### [2026-04-26] 套件改為倉庫根目錄、`cli` 取代 `cmd`、資料路徑調整
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`pyproject.toml`（`package-dir`、`packages` 明列子套件）、`__init__.py`、`__main__.py`、`stage1_validation.py`、多數預設 `Path`、測試路徑、`bsma_equivalence_batch.py`／`bsma_equivalence.py`／`converter/converter.py`、目錄 `cmd/` → `cli/`（含 `mkp.cli.run` 匯入處）、`note/模擬系統.md`
- 主要變更內容：可編輯安裝與匯入 `mkp` 對齊實體目錄；`configs`／`output` 相對專案根；避免根目錄名 `cmd` 與標準庫衝突
- 是否通過測試：`pytest tests/ -m "not slow"` 91 passed
- 備註：新 CLI 模組路徑為 `python -m mkp.cli.run`

### [2026-04-27] 刪除 `mkp/app.py` 轉匯層
**提交人**：AI 協作
**更新摘要**：
- 刪除：`mkp/app.py`（內容已全在 `mkp/cmd/run`）
- 新增：`mkp/__main__.py`（`python -m mkp` → `cmd.run.main`）
- 修改：`mkp/__init__.py`（`main` 延遲載入自 `cmd.run.main`）、`stage1_validation.py`、`tests/test_app.py`
- 是否通過測試：`pytest tests/ -m "not slow"` 全綠
- 備註：請使用 `from mkp.cmd.run import main, ...` 或 `import mkp` 後 `mkp.main`（仍延遲載入）

### [2026-04-27] CLI 入口移至 `mkp/cmd/run/`（刪除根目錄 `main`／`support`）
**提交人**：AI 協作
**更新摘要**：
- 刪除：`mkp/main.py`、`mkp/support.py`
- 新增：`mkp/cmd/__init__.py`、`mkp/cmd/run/main.py`、`mkp/cmd/run/support.py`、`mkp/cmd/run/__init__.py`、`mkp/cmd/run/__main__.py`（`python -m mkp.cmd.run`）
- 修改：`mkp/app.py`、`mkp/__init__.py`、`tests/test_app.py`
- 是否通過測試：`pytest tests/ -m "not slow"` 全綠

### [2026-04-27] `build` 併入 `engine/bundle.py`（移除 `assembly.py`）
**提交人**：AI 協作
**更新摘要**：
- 刪除：`mkp/engine/assembly.py`
- 修改：`mkp/engine/bundle.py`（模組層 `build` 與 `Engine.build` 同檔）、`mkp/engine/__init__.py` 改自 `bundle` 匯出 `build`
- 是否通過測試：`pytest tests/ -m "not slow"` 全綠

### [2026-04-27] 將 `engine` 自單一檔案改為子套件 `mkp/engine/`
**提交人**：AI 協作
**更新摘要**：
- 刪除：`mkp/engine.py`
- 新增：`mkp/engine/__init__.py`、`mkp/engine/bundle.py`（`SimulationBundle`、`Engine`、`build`）、`mkp/engine/defaults.py`（內建 solver 建構子）
- 修改：`mkp/support.py` 改為 `from . import engine` 並以 `engine.build` 呼叫
- 是否通過測試：`pytest tests/ -m "not slow"` 全綠
- 備註：對外仍為 `from mkp.engine import build, Engine, SimulationBundle`

### [2026-04-26 20:30] SimulationBundle 工廠、support.executeSimulator、CLI `--solver`
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/engine.py`（模組層 `build`、`SimulationBundle`：`catalog`、`solver_builders`、`default_rng`、`new_simulator_with_seed`／`NewSimulatorWithSeed`）、`mkp/support.py`（`executeSimulator`、`validate_execute_args`、`preflight_validate`）、`mkp/main.py`（`--solver`）、`mkp/app.py`、`mkp/__init__.py`、`note/模擬系統.md`、`mkp/stage1_validation.py`、多數 `tests/`
- 主要變更內容：`build` 不再建立 `Simulator`；`executeSimulator` 做驗證、build、建 simulator、run_batch、close SHM；CLI 單一 solver
- 是否通過測試：`pytest tests/ -m "not slow"` → 91 passed, 1 deselected
- 備註：程式內 `ExperimentSpec` 仍可使用多個 `solver_ids` 做單元測試，唯 **`validate_execute_args`** 與 CLI 路徑要求單一 solver

### [2026-04-26 17:00] Engine 預掃題庫、ProblemBank（SHM）與 Simulator 查表
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/engine.py`、`mkp/simulator.py`、`mkp/main.py`、`mkp/problem_bank.py`（延續／接線）、`mkp/app.py`（新建，轉匯 `main`）、`tests/test_engine.py`、`tests/test_simulator.py`、`tests/test_simulator_batch.py`、`note/模擬系統.md`
- 主要變更內容：`Engine.build` 掃描 `problem_root`、逐檔驗證、`SimulationBundle` 帶 `game_setting`／`catalog_entries`／`problem_bank`；`Simulator` 以 `ProblemBank.get` 取題，`worker_curriculum` process 路徑以 initializer attach SHM；`main` 於 `run_batch` 後 `problem_bank.close()`；測試補 catalog 失敗、全庫壞檔、`ProblemRepository.load` 於 `run_batch` 後零呼叫。
- 相關 Issue 或對話連結：`log/對話紀錄.md` 本日條目；使用者附件 plan（未修改該檔）
- 是否通過測試：`pytest tests/ -m "not slow"` → 90 passed, 1 deselected（於本機 `.venv`）
- 備註：`Simulator.close()` 釋放 SHM，與 `main` 之 `finally` 雙重呼叫時 `ProblemBank.close` 已容錯

### [2026-04-25 11:28] worker_curriculum 改用多行程優先（CPU-bound 避免 GIL）
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/simulator.py`
- 主要變更內容：`worker_curriculum` 針對內建 solver（`bsma_v1_008`/`stub_solver`）優先使用 `ProcessPoolExecutor`；維持 line 內順序；在主行程統一做 validator + result_writer；測試用自訂 solver 保留 thread fallback
- 相關 Issue 或對話連結：使用者回報「降迭代仍像卡住」，要求直接重跑與修正
- 是否通過測試：`pytest tests/ -m "not slow"` → 87 passed, 1 deselected
- 備註：CPU-bound 負載在 thread 模式受 GIL 限制，容易看似無進度

### [2026-04-25 18:45] 並行批次：ruamel YAML 解析加鎖（修 worker_curriculum 假 ParserError）
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/problem_repository.py`、`mkp/solver_config_loader.py`、`tests/test_problem_repository.py`、`tests/test_solver_config_loader.py`
- 主要變更內容：`YAML().load` 非執行緒安全，多線同時讀題／讀 solver 設定會損壞解析狀態；以 `_yaml_parse_lock` 包住 `load`；補並行單元測試
- 是否通過測試：`pytest tests/ -m "not slow"` → 87 passed, 1 deselected

### [2026-04-25 17:30] worker_curriculum：`repeat` 並行 worker 線（ThreadPoolExecutor）
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/simulator.py`（`run_batch` 依 `repeat_index` 分線並行、`run_task` 校正 `RunResult.repeat_index`）、`mkp/result_writer.py`（寫入鎖）、`mkp/problem_repository.py`（快取鎖）、`tests/test_simulator_batch.py`（Barrier 並行測試）、`note/模擬系統.md`
- 主要變更內容：`worker_curriculum` 下以 `ThreadPoolExecutor(max_workers=repeat)` 同時跑 `repeat` 條 curriculum 線；線內仍串行；回傳結果順序與 `expand_tasks` 一致
- 相關 Issue 或對話連結：使用者期望「repeat = 同時開幾線」
- 是否通過測試：已通過（`pytest tests/ -m "not slow"` → 85 passed, 1 deselected）
- 備註：採用執行緒池非多行程；GIL 下純 Python solver 未必線性加速

### [2026-04-25 16:00] execution_mode：grid / worker_curriculum 與典範種子
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/contracts.py`（`ExecutionMode`、`ExperimentSpec.execution_mode` 與驗證）、`mkp/simulator.py`（`_seed_by_triple`、`expand_tasks` 雙分支）、`mkp/app.py`（`--execution-mode`）、`mkp/__init__.py`（匯出 `ExecutionMode`）、`tests/test_contracts.py`、`tests/test_simulator_batch.py`、`tests/test_app.py`、`note/模擬系統.md`
- 主要變更內容：同一 `(problem_id, solver_id, repeat_index)` 在兩種展開順序下共用同一 seed（`SeedSequence` 依典範排序 spawn）；worker 模式為 `solver × repeat × problem`；預設 grid 行為與既有展開／種子對齊。
- 相關 Issue 或對話連結：使用者交付之「網格與 worker 雙模式」計畫 to-do 清單（勿改 plan 檔）
- 是否通過測試：已通過（`pytest tests/ -m "not slow"` → 84 passed, 1 deselected；既有 BSMA RuntimeWarning）
- 備註：`ExperimentSpec` 呼叫點依 dataclass 預設 `execution_mode="grid"`，無須逐檔顯式填寫

### [2026-04-25 14:30] Engine + SimulationBundle；App 僅 CLI 與 preflight
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：新增 `mkp/engine.py`；`mkp/app.py` 移除內聯組裝改呼叫 `Engine.build`；`mkp/__init__.py` 匯出 `Engine`、`SimulationBundle`；新增 `tests/test_engine.py`；`note/模擬系統.md`（Engine／bundle／主流程步驟 3–4，前述已寫入）
- 主要變更內容：
- `SimulationBundle`（`spec` + `simulator`）；`Engine.build` 建立 Repository、Registry（stub + bsma_v1_008）、ConfigLoader、Validator、ResultWriter、`Simulator`
- `main()`：`preflight_validate` 後 `bundle.simulator.run_batch(bundle.spec)`
- 相關 Issue 或對話連結：計畫「先只做 Engine + bundle」；使用者「繼續」執行實作
- 是否通過測試：已通過（`pytest tests/ -m "not slow"` → 80 passed, 1 deselected；既有 BSMA RuntimeWarning）
- 備註：`tests/test_simulator*.py` 內之 `_build_simulator` 為測試 helper，非已刪之 `app.build_simulator`

### [2026-04-24 23:21] Converter 套件化（mkp.converter）與 dat 轉換註冊表
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：新增 `mkp/converter/__init__.py`、`mkp/converter/register.py`、`mkp/converter/converter.py`；刪除 `mkp/problem_dat_converter.py`；`pyproject.toml` 將 `tool.setuptools.packages.find.include` 調整為 `["mkp*"]` 以納入子套件
- 主要變更內容：
- 以 `register_converter`／`get_converter`／`list_converters` 管理多種 dat 轉換器名稱（預設 `mkp_dat_v1`，並註冊 `weish_dat`、`weing_dat` 等別名）
- 將 `parse_dat_payload`、`ensure_problem_yaml_from_dat`、`load_problem_model_from_repo_dat` 等流程收斂至 `mkp.converter`，全專案改由 `from mkp.converter import ...` 引用
- 新增 `tests/test_converter_registry.py`；`mkp/__init__.py` 維持對外 re-export 與轉換相關 API
- `note/模擬系統.md`：將 **Converter CLI** 與多題庫 parser 擴充列為第二階段（本輪不實作 CLI）
- 相關 Issue 或對話連結：「Converter 套件化重構」計畫
- 是否通過測試：已通過（`pytest tests/ -m "not slow"` → 79 passed, 1 deselected；既有 BSMA divide-by-zero RuntimeWarning）
- 備註：執行 `pip install -e .` 後 `mkp.egg-info/SOURCES.txt` 已更新，不再列出已刪除之 `problem_dat_converter.py`

**變更前後差異說明（選填）**：
- 前版本簡述：單一模組 `problem_dat_converter.py` 承載 dat→YAML／ProblemModel 邏輯
- 後版本簡述：獨立 `mkp.converter` 套件＋註冊表，利於擴充不同題庫 parser 而不影響呼叫端 import 路徑一致性

### [2026-04-22 21:03] 建立核心資料契約與 pytest 驗證基線
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/__init__.py`、`mkp/contracts.py`、`tests/test_contracts.py`
- 主要變更內容：
- 新增四個核心資料契約：`ExperimentSpec`、`ProblemModel`、`RunTask`、`RunResult`
- 在契約層加入最小必要驗證（欄位不可空、數值範圍、維度一致性）
- 對 `ProblemModel` 與 `RunResult` 的陣列欄位設為唯讀，避免後續流程意外修改
- 建立 pytest 測試基線並補上 22 個測試案例，涵蓋合法與非法輸入情境
- 相關 Issue 或對話連結：本次「第一模組落地計畫（核心資料契約）」
- 是否通過測試：已通過（`tests/test_contracts.py` 全數通過）
- 備註：測試環境使用專案本地 `.venv`；系統 Python 因 externally-managed 限制不直接安裝套件

**變更前後差異說明（選填）**：
- 前版本簡述：專案以資料集腳本為主，尚無統一契約與測試基線
- 後版本簡述：已可重用核心資料契約，並具備可持續擴充的 pytest 驗證入口

### [2026-04-22 21:24] 新增 ProblemRepository 與資料載入驗證
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/problem_repository.py`、`mkp/__init__.py`、`tests/test_problem_repository.py`
- 主要變更內容：
- 新增 `ProblemRepository.load(dataset, problem_id)`，支援標準路徑載入與快取
- 使用 `ruamel.yaml` 解析 YAML，並加入 YAML 格式錯誤處理
- 實作 schema 驗證：必要欄位、`values/items`、`weights/items×dim`、`capacities/dim`
- 實作 `best_known: null` fail-fast（在 repository 載入階段即拒絕）
- 新增 UT-PRB-001~005 對應測試（合法載入、缺欄位、維度錯誤、best_known null、快取命中）
- 相關 Issue 或對話連結：本次「第二模組落地計畫（ProblemRepository）」
- 是否通過測試：已通過（`tests/test_problem_repository.py` + `tests/test_contracts.py` 共 31 筆）
- 備註：於專案 `.venv` 補安裝 `ruamel.yaml`

**變更前後差異說明（選填）**：
- 前版本簡述：僅有資料契約，尚無 Problem YAML 載入層
- 後版本簡述：已具備可重用 ProblemRepository 與完整驗證測試，後續可安全銜接 Simulator

### [2026-04-22 21:34] 新增 Solver 介面、Registry 與 Config Loader
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/solver_registry.py`、`mkp/solver_config_loader.py`、`mkp/__init__.py`、`tests/test_solver_registry.py`、`tests/test_solver_config_loader.py`、`tests/test_problem_repository.py`
- 主要變更內容：
- 新增 `Solver` 協定與 `SolverRegistry`（`register/get/create`）
- 新增 `StubMaxIterationsSolver`，用於 UT-SLV 驗證 `max_iterations` 停止邏輯與 `RunResult` 契約
- 新增 `SolverConfigLoader`，使用 `ruamel.yaml` 載入並驗證 `solver_id`、`solver_class`、`stop_condition`、`params`
- 驗證 `stop_condition.type` 僅允許 `max_iterations` / `max_seconds`，並檢查對應數值需大於 0
- 補 `UT-SLV` 對應測試：註冊成功、未註冊拒絕、`RunResult` 欄位、`max_iterations` 停止行為、config schema 驗證
- 修正既有 repository 測試 fixture 縮排錯誤，確保回歸測試穩定
- 相關 Issue 或對話連結：本次「第三模組落地計畫（Solver 介面 + SolverRegistry）」
- 是否通過測試：已通過（solver + repository + contracts 共 40 筆）
- 備註：本輪仍未接舊演算法 adapter，避免與舊腳本耦合

**變更前後差異說明（選填）**：
- 前版本簡述：已有契約與 ProblemRepository，但尚無 solver 統一註冊層
- 後版本簡述：已具備 solver 介面、註冊/建立機制與 config loader，後續可銜接 Simulator

### [2026-04-22 21:41] 新增 Validator 與 ValidationReport
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/validator.py`、`mkp/__init__.py`、`tests/test_validator.py`
- 主要變更內容：
- 新增 `ValidationReport`，集中輸出可行性、objective 驗證、best_known 達標與 gap 資訊
- 新增 `Validator.validate(problem, run_result)`，完成 MKP-specific 驗證邏輯
- feasibility：逐維檢查容量違反並回傳違反維度索引
- objective：重算目標值並偵測 objective mismatch
- best_known：標記達標狀態，並計算 gap（`best_known - best_objective`）
- 新增 `UT-VAL-001~004` 對應 pytest 測試
- 相關 Issue 或對話連結：本次「第四模組落地計畫（Validator）」
- 是否通過測試：已通過（validator + solver + repository + contracts 共 44 筆）
- 備註：本輪未改動 `RunResult` 結構，避免影響既有模組

**變更前後差異說明（選填）**：
- 前版本簡述：缺少集中式 run-level 驗證邏輯
- 後版本簡述：已具備可重用 Validator 與報告結構，可供後續 ResultWriter/Summary 使用

### [2026-04-22 21:50] 新增 ResultWriter 與核心 Summary 聚合
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/result_writer.py`、`mkp/__init__.py`、`tests/test_result_writer.py`
- 主要變更內容：
- 新增 `ResultWriter` 與 `ResultEntry`
- 實作 `write_run(run_result, validation_report)`：將單次結果寫入 `runs.csv` 與 `runs.jsonl`
- 實作 `build_summary(entries)`：聚合核心統計（平均/最佳目標值、可行率、平均 runtime、平均 evaluation_count、排除原因統計）
- 實作 `write_summary(summary)`：寫入 `summary.csv` 與 `summary.json`
- 明確落實排除規則：平均/最佳目標值只納入 `is_feasible=True` 且 `objective_valid=True`
- 落實異常結果保留：infeasible/objective mismatch 仍寫入單次結果輸出
- 新增 `UT-RES-001~003` 對應測試
- 相關 Issue 或對話連結：本次「第五模組落地計畫（ResultWriter / ResultStore）」
- 是否通過測試：已通過（resultwriter + validator + solver + repository + contracts 共 47 筆）
- 備註：本輪 summary 未納入 benchmark 對照欄位（依範圍設定）

**變更前後差異說明（選填）**：
- 前版本簡述：僅有驗證層，尚無固定格式輸出與聚合
- 後版本簡述：已具備單次 run 與核心 summary 的 CSV+JSON 輸出能力

### [2026-04-22 21:56] 新增 Simulator 單次 run 流程
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/simulator.py`、`mkp/__init__.py`、`tests/test_simulator.py`
- 主要變更內容：
- 新增 `Simulator` 並實作 `run_task(task: RunTask)` 單次執行流程
- 串接既有模組：`ProblemRepository` → `SolverConfigLoader` → `SolverRegistry` → `Validator` → `ResultWriter`
- `rng` 由 `np.random.default_rng(task.seed)` 建立，確保 seed 可重現
- `run_task` 回傳 `RunResult`、`ValidationReport`、`ResultEntry`
- 新增單次流程測試：成功流程、seed 可重現、Repository 載入失敗 fail-fast、solver config 載入失敗 fail-fast
- 相關 Issue 或對話連結：本次「第六模組落地計畫（Simulator：單次 run）」
- 是否通過測試：已通過（simulator + resultwriter + validator + solver + repository + contracts 共 51 筆）
- 備註：本輪未實作批次展開與平行執行，遵循單模組邊界

**變更前後差異說明（選填）**：
- 前版本簡述：已有求解/驗證/輸出元件，但未有主流程協調者
- 後版本簡述：已具備可執行的單次 run orchestrator，可作為後續批次與 CLI 入口基礎

### [2026-04-22 22:03] 擴充 Simulator 批次展開與 fail-fast
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/simulator.py`、`tests/test_simulator_batch.py`
- 主要變更內容：
- 新增 `expand_tasks(spec)`：依 `problem_ids × solver_ids × repeat` 展開 `RunTask`
- 新增 seed 預分配：`SeedSequence(base_seed).spawn(n_tasks)`，每個 task 生成獨立可重現 seed
- 新增 `run_batch(spec)`：同步逐筆執行 `run_task`，收集成功結果列表
- 落實 fail-fast：任一 task 發生錯誤立即中止並向上拋出
- 新增批次測試：task 展開、seed 可重現與獨立、problem/config 錯誤 fail-fast
- 相關 Issue 或對話連結：本次「第七模組落地計畫（Simulator：批次展開 + fail-fast）」
- 是否通過測試：已通過（simulator_batch + 既有全回歸共 55 筆）
- 備註：本輪仍未實作平行執行，符合單模組邊界

**變更前後差異說明（選填）**：
- 前版本簡述：僅有單次 `run_task`，無批次任務展開
- 後版本簡述：已具備同步批次展開與 fail-fast 中止策略，可支援完整批次實驗流程

### [2026-04-22 22:15] 新增 Command/App 並打通 CLI 到 run_batch
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/app.py`、`mkp/__init__.py`、`tests/test_app.py`
- 主要變更內容：
- 新增 `build_parser()` 與 flat args 入口
- 新增 `create_experiment_spec()`，將 CLI 參數轉為 `ExperimentSpec`
- 新增 `preflight_validate()`：檢查 problem/solver yaml 是否存在，缺檔直接 fail-fast
- 新增 `build_simulator()`：組裝 Repository/Registry/ConfigLoader/Validator/ResultWriter/Simulator
- 新增 `main()`：正式打通 `CLI -> ExperimentSpec -> Simulator.run_batch`
- 新增 app 測試：合法執行、problem 缺檔、solver 缺檔、必要參數缺失
- 相關 Issue 或對話連結：本次「第八模組落地計畫（Command/App：CLI 到 run_batch）」
- 是否通過測試：已通過（app + 既有全回歸共 59 筆）
- 備註：本輪保留 stub solver 註冊，方便後續替換成真實 solver adapter

**變更前後差異說明（選填）**：
- 前版本簡述：系統模組已齊但缺正式 CLI 入口
- 後版本簡述：已具備可直接命令列執行的最小完整流程，第一階段主幹路徑基本打通

### [2026-04-22 22:20] 接入 BSMA 真實 solver 與重現性驗證
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_v1_008_solver.py`、`mkp/app.py`、`mkp/__init__.py`、`mkp/configs/solvers/bsma_v1_008.yaml`、`tests/test_bsma_solver.py`
- 主要變更內容：
- 新增 `BSMAV1008Solver`，提供 `solve(problem, config, rng) -> RunResult`
- 將 solver 亂數完全改為使用傳入 `numpy.random.Generator`，移除對全域 `random/np.random.seed` 的依賴
- 在 app 組裝層註冊 `bsma_v1_008`，保留 `stub_solver` 以利比較與回退
- 新增 `bsma_v1_008` solver yaml 設定檔（stop_condition + params）
- 新增真實 solver 測試：registry 建立、合法輸出、同 seed 重現、不同 seed 隔離、stop_condition 行為
- 相關 Issue 或對話連結：本次「第九模組落地計畫（接入真實 heuristic + 重現性）」
- 是否通過測試：已通過（bsma + app + simulator + 既有回歸共 64 筆）
- 備註：本輪未修改 `old/*.py`，避免污染舊基線

**變更前後差異說明（選填）**：
- 前版本簡述：僅有 stub solver，可測流程但無真實 heuristic
- 後版本簡述：已可透過正式流程執行第一個真實 heuristic，並具備重現性保證

### [2026-04-22 22:32] 新增第一階段整體驗證 runner（baseline/refactor 對照）
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/stage1_validation.py`、`mkp/__init__.py`、`tests/test_stage1_validation.py`
- 主要變更內容：
- 新增 `ValidationScenario` 與固定 smoke 清單（`WEISH/weish01` + 3 seeds）
- 新增 refactor 執行與同 seed 重跑一致性檢查
- 新增 old script 執行與 baseline objective 收集、差異分類（`match`/`objective_mismatch`/`baseline_missing`）
- 新增 `comparison.json` 與 `summary.txt` 報告輸出
- 新增 dat→yaml 自動轉換流程，補齊 stage1 驗證所需 `mkp/configs/problems/...` 輸入
- 新增對應 pytest 測試，覆蓋清單、重現檢查、差異分類、dat 轉 yaml
- 相關 Issue 或對話連結：本次「第一階段整體驗證與收斂計畫」
- 是否通過測試：已通過（`tests/test_stage1_validation.py` + 全回歸 `68 passed`）
- 備註：實跑產出目錄為 `mkp/output/stage1_validation/20260422_223243`

**變更前後差異說明（選填）**：
- 前版本簡述：系統可執行 batch，但缺少新舊輸出對照與收斂報告流程
- 後版本簡述：已具備第一階段 smoke 驗證 runner，可執行 refactor 基線檢查並輸出比較報告

### [2026-04-22 22:43] 補齊 old 依賴並讓 baseline 比對生效
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/stage1_validation.py`
- 主要變更內容：
- 在專案 `.venv` 安裝 `pandas`、`scipy` 以滿足 `old/main1weish.py` 與 legacy solver 相依
- 調整 `run_old_script` 相容執行流程：加入 legacy 模組 alias（`BSMA -> BSMA2`、`BSCASMA2 -> BSCASMA`）
- 加入 `data\\WEISH` 路徑相容處理，避免舊腳本在 Linux 下初始化失敗
- 加入空 `algorithm_select` fallback，確保 baseline CSV 可產出並被 comparison 流程解析
- 相關 Issue 或對話連結：本次「補齊 old 依賴並重跑驗證」
- 是否通過測試：已通過（`tests/test_stage1_validation.py` + 全回歸 `68 passed`）
- 備註：stage1 實跑輸出 `mkp/output/stage1_validation/20260422_224335`，`baseline_match=3`

**變更前後差異說明（選填）**：
- 前版本簡述：old baseline 無法形成，comparison 全為 `baseline_missing`
- 後版本簡述：old baseline 可形成並可解析，comparison 進入有效對照（本輪 `match`）

### [2026-04-22 22:47] 新增 BSMA 嚴格等價驗證器（不修改 old）
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_equivalence.py`、`mkp/__init__.py`
- 主要變更內容：
- 新增 `verify_equivalence()` 與 `write_equivalence_report()`，對 old/new 在同 seed 下執行逐步 trace 比對
- trace 比對維度包含每步 `sort`/`repair` 的 `digest` 與 `best_fit`，可檢出第一個偏離點
- 輸出報告檔：`mkp/output/stage1_validation/bsma_equivalence_report.json`
- 匯出 `EquivalenceReport`、`verify_equivalence`、`write_equivalence_report` 供外部呼叫
- 相關 Issue 或對話連結：本次「BSMA 嚴格等價驗證（中間過程 + 最終結果）」
- 是否通過測試：已通過（`tests/test_bsma_solver.py`）
- 備註：本次實跑驗證結果為不等價（trace 與 final objective 皆不一致）

**變更前後差異說明（選填）**：
- 前版本簡述：僅能比較最終輸出，不足以判定中間過程一致性
- 後版本簡述：可直接做嚴格等價驗證，並精確定位 first mismatch 步驟

### [2026-04-22 23:02] BSMA 對齊：改為 legacy adapter 並通過嚴格等價
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_v1_008_solver.py`、`mkp/bsma_equivalence.py`
- 主要變更內容：
- 將 `BSMAV1008Solver` 重構為 legacy adapter，直接調用 `old/BSMA2.py::BSMA_V1_008`（不改 old 原檔）
- 新增 `run_seed` 支援，確保嚴格驗證時 old/new 可使用同一 seed
- 將嚴格等價驗證器的新路徑改為對 adapter 執行 trace 比對，確保比較口徑一致
- 實跑報告：`mkp/output/stage1_validation/bsma_equivalence_report.json`
- 本次結果：`final_objective_old=4554`、`final_objective_new=4554`、`final_solution_match=true`、`trace_match=true`
- 相關 Issue 或對話連結：本次「BSMA 對齊模式」
- 是否通過測試：已通過（`tests/test_bsma_solver.py`、全回歸 `68 passed`）
- 備註：驗證案例為 `WEISH/weish01/seed=101/max_iter=30`

**變更前後差異說明（選填）**：
- 前版本簡述：有嚴格驗證能力，但 solver 與 old 在 trace 層仍存在偏移
- 後版本簡述：solver 執行路徑已對齊 legacy，嚴格驗證在指定案例達成完全一致

### [2026-04-23] BSMA：改為內建 Core + equivalence 只掛 Core
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_v1_008_solver.py`、`mkp/bsma_equivalence.py`、`mkp/__init__.py`、`tests/test_bsma_solver.py`
- 新增檔案：`tests/test_bsma_equivalence.py`
- 主要變更內容：
- 新增 `BSMAV1008Core`，邏輯與 `old/BSMA2.py::BSMA_V1_008` 對齊且不 `import old`
- `BSMAV1008Solver.solve` 改為建立 `BSMAV1008Core` 並呼叫 `run()`；`run_seed` 仍可由 config 覆寫
- `bsma_equivalence` 的 new 路徑改為僅暫時替換 `BSMAV1008Core.repair/sort_pop`，確保與 solver 為同一執行本體
- 調整 BSMA 單元測試：移除與舊版不符之可變 `pop_size`，改以固定族群大小 20 驗證 `evaluation_count`
- 相關 Issue 或對話連結：本次「equivalence 與 solver 一致後再對 old」
- 是否通過測試：已通過（全回歸 `69 passed`，含 `tests/test_bsma_equivalence.py`）
- 備註：`WEISH/weish01/seed=101/max_iter=30` 嚴格 trace 仍為一致

**變更前後差異說明（選填）**：
- 前版本簡述：solver 直接委派 old，驗證 new 路徑 patch 的是 `BSMA2` 類別，與「mkp 內實作」口徑不一致
- 後版本簡述：solver 與 equivalence 的 new 端共用 `BSMAV1008Core`，對 old 的比對仍維持獨立讀取 `old/BSMA2`

### [2026-04-23] BSMA Debug：外層迭代摘要與三 seeds 驗收
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_v1_008_solver.py`、`mkp/bsma_equivalence.py`、`tests/test_bsma_equivalence.py`
- 主要變更內容：
- `BSMAV1008Core` 新增可選 `_loop_trace`：每外層迭代記錄 `a`、`b`、`W0_digest`、`i0` 分支、`p_i0` 或 `vb0_digest`/`vc0_digest`、`best_after_sort`
- `verify_equivalence` / `write_equivalence_report` 新增 `include_loop_meta`，回傳 `loop_meta_new` 供 mismatch 診斷
- 新增測試：`WEISH/weish01` 三 seeds（101/202/303）嚴格 trace；`include_loop_meta` 形狀檢查（支援提前達 best_known 而迭代數少於 max_iter）
- `EquivalenceReport` 改為非 frozen 以容納 `loop_meta_new` 列表
- 相關 Issue 或對話連結：本次「BSMA 對齊 Debug 計畫」
- 是否通過測試：已通過（全回歸 `71 passed`）

**變更前後差異說明（選填）**：
- 前版本簡述：僅有 repair/sort trace，難以區分迭代內 RNG 與公式差異
- 後版本簡述：可選擇性輸出每外層迭代摘要，並以三 seeds 固定驗收擴大信心

### [2026-04-23 15:30] WEISH 串流等價驗證、批次 runner 與 pytest slow
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_equivalence.py`、`mkp/bsma_equivalence_batch.py`、`pytest.ini`、`tests/test_bsma_weish_suite_smoke.py`、`tests/test_bsma_weish_suite_slow.py`
- 主要變更內容：
- `verify_equivalence_streaming`：new 端以檔案 `readline` 串流比對 expected digest；`repair`/`sort` 的 `step` 計數與 `_run_new_with_trace` 一致
- `EquivalenceReport` 新增 `new_stop_reason`、`new_reported_eval_count`（串流路徑填入；記憶體路徑為 `None`）
- 新增 `run_weish_equivalence_suite` 與 CLI `python -m mkp.bsma_equivalence_batch`（輸出 `mkp/output/bsma_weish_suite/`）
- `pytest.ini` 註冊 `slow`；smoke 測試小 budget；`test_bsma_weish_suite_slow` 全 30 題 × 10 萬 budget
- 相關 Issue 或對話連結：見「對話紀錄.md」2026-04-23 WEISH 全題庫條目
- 是否通過測試：已通過（`pytest tests/ -m "not slow"` → 73 passed, 1 deselected）
- 備註：`problem_dat_converter` 與 `stage1_validation` 委派為前序對話已完成之共用載入

**變更前後差異說明（選填）**：
- 前版本簡述：大迭代僅能記憶體累積 trace，難以跑滿 30 題 × 高迭代
- 後版本簡述：可串流驗證並有批次摘要與可選全量 slow 測試

### [2026-04-23 16:25] pytest 子目錄收集與 pytest11 插件
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/pytest_plugin.py`、`pyproject.toml`、`pytest.ini`（註解）
- 主要變更內容：pytest 僅在 `invocation_dir == rootpath` 時套用 `testpaths`；於 `pytest11` 註冊 `pytest_load_initial_conftests`，在從子目錄啟動且未指定路徑時插入 `<rootdir>/tests`。
- 相關 Issue 或對話連結：於 `~/mkp/mkp` 執行 `pytest -m slow` 得到 0 tests、`python -m mkp` 找不到套件（後者需 `pip install -e .`）。
- 是否通過測試：已通過（自 `mkp/mkp`：`pytest -m slow --collect-only`；`pytest -m \"not slow\"` 73 passed）
- 備註：安裝後需執行 `pip install -e .` 以註冊 pytest 插件與可編輯套件

### [2026-04-23 16:05] 修正 bsma_equivalence_batch 預設 repo_root
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/bsma_equivalence_batch.py`
- 主要變更內容：CLI 未指定 `--repo-root` 時，由 `Path(__file__).parents[2]` 改為 `parents[1]`，使預設指向含 `data/WEISH` 的專案根（`/.../mkp`），避免誤用使用者家目錄。
- 相關 Issue 或對話連結：使用者執行 `python -m mkp.bsma_equivalence_batch` 出現 `Missing dat file: /home/sean/data/WEISH/...`
- 是否通過測試：已手動確認 `dat_file_path` 指向 `.../mkp/data/WEISH/weish01.dat` 且檔案存在
- 備註：仍可用 `--repo-root` 覆寫

### [2026-04-24 09:40] Summary 模組完善：分組統計與批次自動落檔
**提交人**：AI 協作
**更新摘要**：
- 修改檔案：`mkp/result_writer.py`、`mkp/simulator.py`、`tests/test_result_writer.py`、`tests/test_simulator_batch.py`
- 主要變更內容：
- `ResultEntry` 新增 `best_known`、`best_known_reached`、`best_known_gap` 欄位；`excluded_reason` 加入 `runtime_error` 並採優先序 `infeasible > objective_mismatch > runtime_error`
- `build_summary` 改為輸出 `overall` + `by_problem_solver`（run_count、valid_run_count、avg/best objective、feasible_rate、avg runtime/eval、excluded_counts、best-known 聚合）
- `write_summary` 改為 JSON 全量保留 + CSV 攤平列（`row_type=overall/group`）
- `Simulator.run_batch` 完成後自動呼叫 `build_summary` + `write_summary`
- 更新測試覆蓋新 summary 結構、runtime_error 統計與批次自動落檔
- 相關 Issue 或對話連結：見「對話紀錄.md」2026-04-24 Summary 模組完善條目
- 是否通過測試：已通過（`tests/test_result_writer.py` + `tests/test_simulator_batch.py`：9 passed；`pytest tests/ -m "not slow"`：75 passed, 1 deselected）
- 備註：保留 fail-fast 契約；例外時不吞錯
